# Phase 0 Codex — Famille 37 · Rangement modulaire & mobilier transformable

- Run : `20260720-124517`
- Base : SEMrush France, EUR
- Requêtes contrôlées : `console extensible`, `lit escamotable`, `table basse relevable`

## OBSERVÉ — console extensible

Le cluster commercial propre dépasse **27 300 recherches/mois** : `console extensible` et `table console extensible` portent chacune 9 900 recherches, puis `console extensible table` 3 600, `console table extensible` 2 900 et `tables consoles extensibles` 1 000. Le CPC de la tête est de **0,33 EUR**. Le cluster passe vers la sonde prix et concurrence.

## OBSERVÉ — lit escamotable

La tête `lit escamotable` atteint **27 100 recherches/mois** et `lit escamotable 140x190` 4 400. Le CPC de la tête est de **0,47 EUR**. Le cluster passe vers la sonde prix, avec alerte sécurité d'ancrage et logistique.

## MANQUANT — table basse relevable

La lecture SEMrush de cette requête a été interrompue techniquement. Aucun volume n'est repris ni estimé dans la décision finale. La piste n'est donc pas comptée.

## Gate

Console extensible et lit escamotable passent vers la sonde concurrence. La table basse relevable reste non qualifiée, sans extrapolation.

# Rapports Codex

Les rapports sont nommés `<stage>-<family_id>-<cluster_id>-<run_id>-a<attempt>.md`. Ils sont append-only : une nouvelle tentative crée un nouveau fichier et ne remplace pas la preuve précédente.

Étapes attendues : `phase0`, `sonde-prix`, `phase2`, `phase3`, `phase4`, `phase5`, `critique`, puis un dossier consolidé par candidat.


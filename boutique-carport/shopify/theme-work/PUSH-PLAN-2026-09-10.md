# Plan de push — Sous Abri (thème brouillon 199695565183, copie non publiée)

Préparation LOCALE uniquement (aucun appel Shopify effectué pour produire ces fichiers). Gabarit repris de `boutique-cosycathouse/shopify/theme-work/` (même thème FullStack). Vérifier chaque empreinte md5 après push, comme pour Cosy Cat House.

## 1. Ordre de push (`themeFilesUpsert`, body TEXT)

1. `assets/sa-motion.css`
2. `sections/sa-reviews.liquid`
3. `config/settings_data.json` ← `theme-work/settings_data.json` (Archivo/Inter, palette terre cuite `#C8552B` / ardoise `#2F4A5A` / fond `#F4F1EA`)
4. `sections/header-group.json`
5. `sections/footer-group.json`
6. `templates/index.json` (11 sections : sa_head, hero, pain, mecanisme, familles, declarer, comparaison, reassurance, avis (vide), faq, cta_final — 9 sections de contenu selon `content/pages/accueil.md`, + section utilitaire CSS + section avis conservée vide)
7. `templates/product.json` (11 sections : sa_head, main, benefit1-6, comparaison, faq, cta) — **gabarit générique pour les 10 fiches à prix fixe**, piloté par `descriptionHtml` + metafields `custom.*` (liste en §3)
8. `templates/page.json` (générique, réutilisé tel quel depuis Cosy Cat House — déjà 100 % générique, aucune copie CCH dedans)
9. `templates/page.contact.json` (formulaire de contact natif Shopify, aucune app)
10. `templates/page.devis.json` (3 pages sur devis, formulaire natif Shopify avec les champs longueur/largeur/hauteur/voitures/configuration/toit/toiture/coloris/sol/CP/e-mail/téléphone/case démarches)
11. `templates/collection.json` (générique pour les 11 collections)

Contrôles après push : accueil et fiche produit en préview mobile 375 px et desktop ; **aucun** chiffre d'avis, aucune note, aucun prix barré, aucune urgence ; police Archivo sur les titres ; formulaire de contact et formulaire de devis soumettent bien (test avec une fausse valeur, vérifier l'e-mail reçu côté marchand) ; comparatif lisible sur mobile (tableau scrollable horizontalement) ; section « Avis clients » vide et sans distorsion visuelle tant qu'aucun bloc `review` n'est ajouté.

## 2. Contenu jamais à publier tel quel

Aucun avis, aucune note (chasse gardée de Hakim), aucun prix barré, aucune urgence fabriquée (compte à rebours, « plus que X en stock »). Le custom-code `cch_head` de Cosy Cat House (injection JS `£119 → £89`) n'a **pas** été repris : `sa_head` ne contient que le lien vers `sa-motion.css`.

## 3. À créer côté Shopify avant le push des templates

### Metafields produit (namespace `custom`), à remplir depuis `content/produits/*.md` pour chacune des 10 fiches à prix fixe
- `custom.sous_titre` (texte court) — sous-titre H1
- `custom.specifications` (rich text) — fusion des tableaux « Dimensions et capacité » + « Caractéristiques »
- `custom.delai_livraison` (texte court) — ex. « 5 à 12 jours ouvrés » ou « 8 à 12 semaines »
- `custom.contenu_colis` (rich text, liste) — contenu du colis
- `custom.protege` (rich text) — section « Ce qu'il protège vraiment »
- `custom.declarer` (rich text) — section « Faut-il le déclarer ? »
- `custom.montage_texte` (rich text) — section « Le montage, honnêtement »
- `custom.ancrage_vent` (rich text) — section « L'ancrage et le vent »
- `custom.ne_fait_pas` (rich text) — section « Ce qu'il ne fait pas »
- `custom.livraison_texte` (rich text) — section « La livraison, concrètement »
- `custom.faq` (rich text HTML, blocs `<details><summary>Question</summary><p>Réponse</p></details>`) — FAQ produit (8-10 questions par fiche)

### Metafields collection (namespace `custom`), à remplir pour les 11 collections
- `custom.texte_seo` (rich text) — le « Texte SEO (sous la grille) » de chaque `content/collections/*.md` (le « Texte d'intro » va déjà dans `descriptionHtml`, cf. `shopify/mut-collections.json`)

### Metafields page (namespace `custom`), pour les 3 pages sur devis
- `custom.sous_titre` (rich text)
- `custom.faq` (rich text HTML, même convention `<details>` que les produits)

### Pages à créer (template `page.json` sauf mention contraire)
`guide-declaration-carport`, `faq`, `qui-sommes-nous`, `garantie-livraison-sav`, `comparatif-carport-tente-garage` (contenu déjà rédigé dans `content/pages/*.md`), `contact` (template `page.contact.json`), et les 3 pages sur devis avec template `page.devis.json` : `devis-carport-aluminium-toit-plat`, `devis-carport-aluminium-adosse`, `devis-carport-cintre-2-pieds`.

### Menus de navigation
- `main-menu` (5 entrées imposées par le brief) : Carports aluminium → `/collections/carport-alu` · Tentes-garages → `/collections/carport-metallique` **[À VÉRIFIER : aucune collection « tentes-garages » dédiée parmi les 11 créées (`shopify/mut-collections.json`) — confirmer ce lien ou créer la collection manquante]** · 2 voitures → `/collections/carport-2-voitures` · Camping-car → `/collections/carport-camping-car` · Guides (sous-menu) → `guide-declaration-carport`, `guide-montage-ancrage`, `comparatif-carport-tente-garage`.
- `footer-aide` : FAQ, Contact, Livraison et délais → `garantie-livraison-sav`, Retours et rétractation → politique de remboursement native, Garantie → `garantie-livraison-sav`.
- `footer-apropos` : Qui sommes-nous, Mentions légales → politique native, CGV → politique native, Politique de confidentialité → politique native.

### Politiques natives Shopify à coller
Contenu dans `content/politiques/politiques-a-coller.md` → Politique de livraison, Politique de retour, Conditions générales de vente, Politique de confidentialité, Mentions légales.

### Images à téléverser (`shopify://shop_images/...`, tous en placeholders dans ces fichiers)
- Logo : `logo-sousabri.png`, `logo-sousabri-inverse.png` **[À VÉRIFIER : logo pas encore produit]**
- Lot 1 (accueil, 7 images) et Lot 3 (pages devis + guides, 9 images) : voir `shot-list.md` — noms déjà livrés en JPG dans `livraisons/visuels-lot1-2026-09-09/` sauf `sousabri-hero.jpg` et `sousabri-hero-mobile.jpg`, pas encore produits **[À VÉRIFIER]**
- Lot 2 (galeries produit, 50 images, 5 par fiche : face/situation/detail-structure/montage/ancrage) : pas encore produites, voir `ordres-en-attente/` — `product.json` référence les images de galerie par position (`closest.product.images[0..4]`), donc l'**ordre d'upload doit respecter exactement** face, situation, detail-structure, montage, ancrage pour chaque fiche.

### Paiement
Vérifier dans Shopify Payments que carte, PayPal (4×), Klarna (3×), Apple Pay et Shop Pay sont bien actifs, pour que le bloc `payment-methods` (icônes automatiques) corresponde au texte du pied de page.

### Polices
`settings_data.json` a été généré par `scripts/tokens_to_theme.py` puis corrigé à la main : le script écrit le nom brut de la police (« Archivo », « Inter ») alors que ce thème FullStack attend un identifiant `famille_nPOIDS` (ex. `archivo_n7`) — corrigé ici en `archivo_n7` (titres), `archivo_n6` (sous-titres), `inter_n4` (corps). À reconfirmer dans l'éditeur de thème que ces polices Google sont bien disponibles dans la liste FullStack.

## 4. Décisions restant à Hakim

- Lien de la carte famille « Tentes-garages » (accueil + mécanisme) : pas de collection `tentes-garages` dédiée parmi les 11 créées — pointé provisoirement vers `carport-metallique`.
- Lien de la carte famille « Carports sur mesure (devis) » : pointé provisoirement vers `carport-adosse`, à confirmer.
- CTA final de `product.json` (bouton « Voir le tarif et commander ») : lien `#`, à remplacer par une ancre réelle vers le bloc d'achat une fois le thème poussé en ligne (l'ID DOM de `main-product` n'est pas vérifiable en local).
- Upload des photos (pièce jointe) sur le formulaire de contact natif : à confirmer si ce thème/Shopify le permet nativement ; sinon le texte du formulaire renvoie déjà vers une réponse par e-mail.

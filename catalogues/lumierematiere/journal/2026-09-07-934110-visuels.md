# 934110 — visuels un tube et deux tubes

Date : **07/09/2026** · handle : `suspension-bois-led-934110` · livraison locale uniquement.

## Résultat

Deux JPEG RGB **2048 × 2048** ont été livrés dans
`livraisons-visuels-codex/2026-09-07-934110/` :

- `lm5-934110-un-tube.jpg` sert `200000531:193#Yellow Travertine` ; **un tube** observé ;
- `lm5-934110-deux-tubes.jpg` sert `200000531:173#3000k-warm white` et
  `200000531:175#6000k-cold white` ; **deux tubes** observés sous une rosace commune.

La planche `qa.jpg` et `production.json` accompagnent les deux images. Aucune image fournisseur
brute n'est livrée et aucun texte, cote, logo ou watermark n'est présent dans les JPEG produit.

## Correspondance et fidélité

La correspondance vient des identifiants du SKU et de `preuves-dom.json`. La référence `193`
montre un tube Ø 4 × H 28 cm ; la référence `173` montre deux tubes identiques à des hauteurs
différentes, sous une rosace Ø 10 × H 2,5 cm. Les références `173` et `175` ont le même SHA-256
`a3a84510f4ebc3a89a4e6a9d25fab17e8e1991f5aafb241ab62aa3247f184953` : un seul visuel les sert.

Les deux rendus gardent le même composite beige à grain minéral. Les sources lumineuses sont
présentées éteintes : aucune différence visuelle 3000 K/6000 K n'est fabriquée.

## QA

- `un tube` : SHA-256 `24ba50bd4f5591d5b6d98b41831abc4aeca44e5e45c4d4d62a482289d7158964` ;
- `deux tubes` : SHA-256 `8a7585ba75a7dce1a145c65e32518d27c61de1c2e8a9bcc2b1972bdb5439e176` ;
- dimensions, mode et format : **2048 × 2048, RGB, JPEG** pour les deux ;
- inspection pleine résolution : **PASS**, exactement 1 et 2 tubes, un seul luminaire par image.

Génération : `image_gen` intégré à partir de chaque référence exacte, puis conversion JPEG qualité
95 avec `sips`. Aucun import Shopify/DSers, aucune mutation de SKU et aucune vérification publique
de l'affichage par variante dans cette passe.

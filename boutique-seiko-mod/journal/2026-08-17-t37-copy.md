---
type: journal
boutique: seiko-mod
date: 2026-08-17
nature: intervention
leviers: [page]
titre: "T-37 — purge des tirets cadratins sur le copy public"
---

# T-37 — purge des tirets cadratins sur le copy public

> **17/08/2026 ~13h00.** Boutique Maison Noirmont (`v42pzp-h4`). Hakim : le copy
> sent l'IA, notamment les longs tirets (capture : « emprunté — et l'allure » sur
> La Maison). Appliquer `STYLE-REDACTION.md`. Auth CLI : `contact.noirmont@gmail.com`.
> Aucun handle touché, aucun média déplacé, aucun brouillon activé.

## Écrit

Passe de **ponctuation**, pas une réécriture SEO (ça reste T-31). Les faits produit
sont inchangés. 0 `userErrors`.

| Surface | Avant | Après | Mutations |
|---|---|---|---|
| 96 fiches actives (titre, description, meta title) | 81 + 145 + 72 cadratins | **0** | 93 `productUpdate` (3 fiches déjà propres) |
| 22 collections avec cadratin (publiques et pièces 404) | descriptions | **0** | 22 `collectionUpdate` |
| Pages publiées `la-maison`, `faq`, `configurateur`, `politique-de-cookies` | 2 / 6 / 1 / 11 | **0** | 4 `pageUpdate` |
| Thème MAIN `205451100498` | déjà purgé le 15/08 | 1 cadratin restant, **dans un commentaire Liquid** (invisible) | 0 |

Règle appliquée : ` — et/ou/mais… ` → virgule ; apartés ` — … — ` → parenthèses ;
titres `Nom — sous-titre` → `Nom : sous-titre` ; le reste → deux-points.

La phrase de la capture, live :

> un cadran vierge de tout nom emprunté, et l'allure qui va avec.

Les autres marqueurs de `STYLE-REDACTION.md` (« que vous soyez », « plongez dans »,
tricolons, « véritable / incontournable ») étaient **déjà absents** des 96 fiches.

## Non touché, volontairement

- **`alt` des images** : encore le gabarit « … — Maison Noirmont ». Ce n'est pas le
  copy lu sur la page. T-37 interdisait de toucher aux médias. À faire à part si
  Hakim le veut (l'`alt` est une propriété du fichier, partagée entre fiches : T-36).
- Pages CMS **dépubliées** (doublons de politiques) : le copy légal live est dans
  les `shopPolicy`, pas dans ces pages.
- Brouillons de repeuplement : déjà sans cadratin depuis T-63.
- T-38 (gabarit de titre dans le journal T-21) : document interne, pas le site.

## Preuves

`backups/2026-08-17-t37-copy/` : `products-avant.json` / `products-apres.json`,
plans, `mutation-log.json` (0 erreur). Scan Admin après : **96/96 fiches à 0 cadratin**
sur titre + description + SEO.

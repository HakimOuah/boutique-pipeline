---
type: journal
boutique: bonum-vitae
date: 2026-08-17
nature: intervention
leviers: [autre]
titre: "17/08/2026 (soir) — Rail A : les P0 sont retirés du live"
---

# 17/08/2026 (soir) — Rail A : les P0 sont retirés du live

> Boutique Bonum Vitae (`kw7vak-g0`). Auth CLI device-code faite par Hakim en début de session
> (`contact@bonumvitae.fr`). Écritures limitées au crible sur le thème MAIN Horizon
> (`203569004882`), conformément au rail A. **Aucun thème publié, aucun produit créé ou supprimé.**

## Fait et vérifié

### T-H1 — auth CLI ✅

`shopify store auth --store kw7vak-g0.myshopify.com` avec les scopes `write_products, write_files,
write_themes, write_content, read_online_store_pages, write_legal_policies`. Confirmé :
`shop { name }` = Bonum Vitae. Les sessions Tuftéo et Noirmont sont intactes.

### T-H2 — FullStack présent ✅

`copie-de-fullstack-2-3`, id **205568147794**, UNPUBLISHED, créé le 17/08 15h35. 250+ fichiers :
c'est le **zip vendeur brut** (`logo-fullstack.png`, `powered-by-fullstack.svg`, placeholders) —
la purge démo reste à faire avant toute préview publique (rail B). Autres thèmes : Horizon MAIN
`203569004882`, deux `theme-impact-tristan` unpublished de juillet.

### T-01 + T-02 — faux avis et « 4.8/5 · 312 avis » retirés ✅

La section `bv-avis-clients` (Claire M. / Karim B. / Bernard L. « Vérifié ») était rendue par
**trois templates** ; le compteur « 4.8/5 basé sur 312 avis vérifiés » était un bloc
`custom_liquid_bhBnde` imbriqué dans `product-details` :

| Fichier | Retiré |
|---|---|
| `templates/index.json` | section `bv_avis_clients_NiGLt3` |
| `templates/product.json` | section `bv_avis_clients_8zemYP` + bloc `custom_liquid_bhBnde` |
| `templates/product.osmoseur.json` | section `bv_avis_clients_8zemYP` + bloc `custom_liquid_bhBnde` |

Le suffixe `osmoseur` n'est porté que par `osmoseur-ro-600g` (`templateSuffix` scanné sur les 26
fiches) — c'est pour ça que la première passe laissait la fiche osmoseur sale.

**Incident évité** : le premier nettoyage de `product.osmoseur.json` supprimait par erreur le bloc
parent `product-details` entier (le filtre matchait le JSON sérialisé du parent). Refait depuis le
backup en ciblant le seul bloc feuille. Rien de cassé n'a été poussé ; « Ajouter au panier »
vérifié présent après coup.

### T-03 — prix barrés purgés ✅

Scan admin complet : **26 produits** (24 ACTIVE, 2 DRAFT), **8 variantes** avec `compareAtPrice` —
les 6 publiques connues **plus 2 dormantes** sur le brouillon
`osmoseur-de-cuisine-shuangli-600g-osmose-inverse` (406,90/500 et 380,90/480) : la bombe type
Noirmont T-50 existait bien. 7 mutations `productVariantsBulkUpdate`, **0 `userErrors`**, valeurs
d'origine sauvegardées. Re-scan : **0 `compareAtPrice` non nul** sur tout le catalogue.

### T-04 — bandeau « Offre d'été : -20% » retiré ✅

`sections/header-group.json`, bloc `announcement_2` supprimé. Le bloc `announcement_1`
(« Livraison offerte, sans minimum d'achat ») est **conservé** — cohérent avec la description boutique
et à recouper avec la policy livraison (T-06).

## Preuves

- **API** : relecture des 4 fichiers distants, identiques au local au md5 près, hors deux
  normalisations Shopify : handle d'app `trustwill-trustoo` réécrit `cwilltrustoo-reviews`, et un
  `\n` final. `templates/product.osmoseur.json` distant : 83 810 octets, 0 « 312 avis »,
  0 `bv_avis_clients`, `product-details` intact.
- **Public (anonyme, après purge du cache storefront ~2 min)** :
  - Accueil : 0 « Vérifié », 0 Claire/Karim/Bernard, 0 -20 %, 0 prix barré.
  - `/products/osmoseur-ro-600g` : PROPRE (avis, 4.8/5, 312, 470,00 absents ; « Ajouter au panier » présent).
  - `/products/detartreur-super-magnetique-ipse-maison-dn20` : PROPRE.
- **Backups** : `backups/2026-08-17-rail-a/` — les 5 fichiers thème d'origine +
  `compare-at-avant-purge.json`.

## Constaté au passage

- **App d'avis Trustoo présente** (`cwilltrustoo-reviews`) : bloc `review-widget` dans la section
  `17839418829d5d56fb` des deux templates produit. Avec 0 commande il ne devrait rien rendre — à
  surveiller, et à trancher au rail B.
- Le piège cache storefront s'est reproduit : la fiche osmoseur a servi l'ancien HTML ~2 minutes
  après l'écriture (taille au octet près identique). La preuve fiable est passée par l'API d'abord,
  le public ensuite.

## Ce que je n'ai pas pu vérifier

- **Apps installées / GMC existant** : `appInstallations` refuse (`ACCESS_DENIED`, scope
  `read_apps` non demandé). Deux options : Hakim regarde Admin → Canaux de vente (app Google &
  YouTube ?), ou re-auth avec `read_apps` en plus.
- Moyens de paiement réels (`shop.enabled_payment_types` est du Liquid, pas de l'Admin API — se
  lira au rail B sur la préview FullStack).
- Filigranes / photos brutes : toujours pas d'audit visuel fiche par fiche.

## Reste ouvert (rail A élargi / rail B)

P1 non traités ce soir : hero « sans travaux ni plombier », newsletter -10 % (code réel ?), FAQ
24-48 h vs policies, doublon mentions légales, footer sans raison sociale, collections < 5,
handle `eau-adoucie`. Tickets T-06, T-07 au tableau. Rail B : T-08 persona → T-09 inventaire
FullStack → DA.
